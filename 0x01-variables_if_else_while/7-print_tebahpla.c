#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ahlpa;

	for (ahlpa = 'z'; ahlpa >= 'a'; ahlpa--)
	putchar(ahlpa);
	putchar('\n');

	return (0);
}
