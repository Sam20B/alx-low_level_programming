#!/bin/bash

# Get the C file name from the user
read -p "Enter the name of the C file: " CFILE

# Run the C file through the preprocessor and save the output to a file called c
cpp $CFILE > c

# Print a success message
echo "Preprocessing complete. Output saved to c."

